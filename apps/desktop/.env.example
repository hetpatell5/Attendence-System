# Electron/Vite renderer configuration.
# This file must NEVER contain database credentials or backend secrets —
# the desktop app only ever talks to the API over HTTPS.

VITE_API_URL=http://localhost:3000
